<Window x:Class="PipeWiseClient.Windows.NumericRangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="הגדרת טווח מספרי" Height="230" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#FAFAFA"
        FlowDirection="RightToLeft">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
    <TextBlock x:Name="HelpText" Grid.Row="1" Foreground="#555" Margin="0,0,0,12"/>

    <Grid Grid.Row="2" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="160"/>
        <ColumnDefinition Width="20"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="160"/>
      </Grid.ColumnDefinitions>

      <TextBlock Text="גבול תחתון:" VerticalAlignment="Center"/>
      <TextBox x:Name="MinBox" Grid.Column="1" Margin="8,0,0,0" />
      <TextBlock Text="גבול עליון:" Grid.Column="3" VerticalAlignment="Center"/>
      <TextBox x:Name="MaxBox" Grid.Column="4" Margin="8,0,0,0" />
    </Grid>

    <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center">
      <TextBlock Text="חריגה מהטווח:" VerticalAlignment="Center" Margin="0,0,8,0"/>
      <ComboBox x:Name="ActionCombo" Width="180">
        <ComboBoxItem Content="הסר שורה (מומלץ)" Tag="remove" IsSelected="True"/>
        <ComboBoxItem Content="החלף בערך" Tag="replace"/>
        <ComboBoxItem Content="רק התרעה (השאר)" Tag="warn"/>
      </ComboBox>
      <TextBlock Text="ערך חלופי:" VerticalAlignment="Center" Margin="16,0,8,0"/>
      <TextBox x:Name="ReplacementBox" Width="120" IsEnabled="False"/>
    </StackPanel>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Content="אישור" Width="90" Margin="0,0,8,0" Click="Ok_Click"/>
      <Button Content="ביטול" Width="90" Click="Cancel_Click"/>
    </StackPanel>
  </Grid>
</Window>
