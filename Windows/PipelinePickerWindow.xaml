<Window x:Class="PipeWiseClient.Windows.PipelinePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="בחר פייפליין שמור" Height="480" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- חיפוש -->
        <DockPanel Grid.Row="0" LastChildFill="True">
            <!-- כפתור רענון מוצמד לימין -->
            <Button x:Name="RefreshBtn" Content="רענן" Width="80"
                    DockPanel.Dock="Right" Click="Refresh_Click" />

            <!-- תיבת חיפוש + פלייסהולדר שקוף כשיש טקסט -->
            <Grid Margin="0,0,8,0" VerticalAlignment="Center" Width="260">
                <TextBox x:Name="SearchBox" />
                <TextBlock Text="חפש לפי שם"
                           IsHitTestVisible="False"
                           Margin="6,0,0,0"
                           Foreground="Gray"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </DockPanel>

        <!-- רווח -->
        <Border Grid.Row="1" Height="8" Background="Transparent"/>

        <!-- רשימת פייפליינים -->
        <ListBox x:Name="PipelinesList" Grid.Row="2" DisplayMemberPath="name"
                 MouseDoubleClick="PipelinesList_MouseDoubleClick"/>

        <!-- רווח -->
        <Border Grid.Row="3" Height="8" Background="Transparent"/>

        <!-- כפתורי פעולה -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="בחר" Width="90" Margin="0,0,8,0" IsDefault="True" Click="Choose_Click"/>
            <Button Content="בטל" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
