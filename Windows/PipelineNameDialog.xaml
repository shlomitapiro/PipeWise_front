<Window x:Class="PipeWiseClient.Windows.PipelineNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="שם לפייפליין" Height="160" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        ShowInTaskbar="False">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="10"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="בחר שם לפייפליין (ישמש לחיפוש ברשימה)"
               FontSize="13" Margin="2,0,2,4"/>

    <!-- תיבת טקסט עם פלייסהולדר פשוט -->
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="10"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="10"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <Grid>
        <TextBox x:Name="NameBox" Height="30" VerticalContentAlignment="Center"/>
        <TextBlock Text="למשל: Customers – ניקוי וטרנספורמציה"
                   Margin="6,0,0,0" Foreground="Gray" IsHitTestVisible="False"
                   VerticalAlignment="Center">
          <TextBlock.Style>
            <Style TargetType="TextBlock">
              <Setter Property="Visibility" Value="Collapsed"/>
              <Style.Triggers>
                <DataTrigger Binding="{Binding Text, ElementName=NameBox}" Value="">
                  <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
              </Style.Triggers>
            </Style>
          </TextBlock.Style>
        </TextBlock>
      </Grid>

      <Button Grid.Column="2" Content="אישור" Width="90" Height="30" IsDefault="True"
              Click="Ok_Click" Margin="0,0,0,0"/>
      <Button Grid.Column="4" Content="בטל" Width="90" Height="30" IsCancel="True"/>
    </Grid>
  </Grid>
</Window>


