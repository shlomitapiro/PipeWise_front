<Window x:Class="PipeWiseClient.Windows.RemoveInvalidDatesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="הסרת תאריכים לא חוקיים" Height="260" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#FAFAFA" FlowDirection="RightToLeft">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock FontSize="16" FontWeight="SemiBold"
               Text="הגדרות 'הסר תאריך לא חוקי' לעמודה" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0">
      <TextBlock Text="טווח שנים:" VerticalAlignment="Center" Margin="0,0,8,0"/>
      <TextBlock Text="מינימום" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox x:Name="MinYearBox" Width="80" Margin="0,0,10,0"/>
      <TextBlock Text="מקסימום" VerticalAlignment="Center" Margin="0,0,6,0"/>
      <TextBox x:Name="MaxYearBox" Width="80"/>
      <TextBlock Text="(אופציונלי)" Foreground="#666" VerticalAlignment="Center" Margin="8,0,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
        <TextBlock Text="טווח תאריכים:" VerticalAlignment="Center" Margin="0,0,8,0"/>
        <TextBlock Text="מינימלי" VerticalAlignment="Center" Margin="0,0,6,0"/>
        <TextBox x:Name="MinDateBox" Width="120" Margin="0,0,16,0"
                ToolTip="לדוגמה: 2020-01-01 או 01/01/2020"/>
        <TextBlock Text="מקסימלי" VerticalAlignment="Center" Margin="0,0,6,0"/>
        <TextBox x:Name="MaxDateBox" Width="120"
                ToolTip="לדוגמה: 2030-12-31"/>
        <TextBlock Text="(אופציונלי)" Foreground="#666" VerticalAlignment="Center" Margin="8,0,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Margin="0,12,0,0">
      <TextBlock Text="טיפול בערכים ריקים:" FontWeight="SemiBold"/>
      <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
        <RadioButton x:Name="EmptyRemoveRadio" Content="הסר שורה" IsChecked="True" Margin="0,0,18,0"/>
        <RadioButton x:Name="EmptyReplaceRadio" Content="השלם בתאריך:"/>
        <TextBox x:Name="EmptyReplacementBox" Width="160" Margin="6,0,0,0" IsEnabled="False"
                 ToolTip="הכנס תאריך תקין (למשל 01/02/2025 או 2025-02-01T10:00:00)"/>
      </StackPanel>
      <TextBlock Text="הערה: מתקבלים הפורמטים הנתמכים וה-ISO (כולל Z)."
                 Foreground="#666" Margin="0,6,0,0"/>
    </StackPanel>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,16,0,0">
      <Button Content="אישור" Width="90" Margin="0,0,8,0" Click="Ok_Click"/>
      <Button Content="ביטול" Width="90" Click="Cancel_Click"/>
    </StackPanel>
  </Grid>
</Window>
