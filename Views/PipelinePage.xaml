<Page x:Class="PipeWiseClient.Views.PipelinePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="PipeWise - Run Pipeline">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- File Selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Input File:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox x:Name="FilePathTextBox" Width="400" IsReadOnly="True"/>
            <Button Content="Browse..." Click="BrowseFile_Click" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Pipeline Controls -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Button Content="Load Config" Width="120" Click="LoadConfig_Click"/>
            <Button Content="Save Config" Width="120" Margin="10,0,0,0" Click="SaveConfig_Click"/>
            <Button Content="Run Pipeline" Width="120" Margin="10,0,0,0" Click="RunPipeline_Click"/>
        </StackPanel>

        <!-- Result Preview / Status -->
        <TextBox x:Name="ResultTextBox"
                 Grid.Row="2"
                 Text="Result or status will appear here"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Page>
