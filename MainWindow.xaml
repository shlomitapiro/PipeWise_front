<Window x:Class="PipeWiseClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PipeWise - ETL Client" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- סגנונות מותאמים אישית -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357ABD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ColumnPanel" TargetType="Border">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E1E5E9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!-- כותרת ראשית -->
        <TextBlock Grid.Row="0" Text="PipeWise - מערכת עיבוד נתונים חכמה" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- בחירת קובץ מקור -->
        <Border Grid.Row="1" Style="{StaticResource ColumnPanel}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📁 בחר קובץ מקור" Style="{StaticResource SectionHeader}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="FilePathTextBox" Width="600" Height="35" 
                             IsReadOnly="True" FontSize="12" 
                             VerticalContentAlignment="Center" 
                             Background="White" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    <Button Content="Browse" Click="BrowseFile_Click" 
                            Style="{StaticResource ModernButton}" 
                            Margin="10,0,0,0" Height="35"/>
                </StackPanel>

                <!-- בחירת סוג מקור: קובץ / MySQL -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0" VerticalAlignment="Center">
                    <TextBlock Text="Source: " VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="SourceTypeCombo"
                              Width="160"
                              SelectedIndex="0"
                              SelectionChanged="SourceTypeCombo_SelectionChanged">
                        <ComboBoxItem Content="File (CSV/JSON/Excel)"/>
                        <ComboBoxItem Content="MySQL"/>
                    </ComboBox>
                </StackPanel>

                <TextBlock x:Name="FileInfoTextBlock" 
                           Text="לא נבחר קובץ" 
                           Foreground="#7F8C8D" 
                           FontStyle="Italic" 
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- אזור פרטי MySQL (מוסתר כברירת מחדל) -->
        <Border Grid.Row="2" Style="{StaticResource ColumnPanel}" Margin="0,0,0,15" x:Name="MySqlPanel" Visibility="Collapsed">
            <StackPanel>
                <TextBlock Text="🗄️ MySQL Connection" Style="{StaticResource SectionHeader}"/>
                <Grid Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Host:" VerticalAlignment="Center"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" x:Name="MySqlHostTextBox" Margin="8,0,0,6" Text="localhost"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="User:" VerticalAlignment="Center"/>
                    <TextBox   Grid.Row="1" Grid.Column="1" x:Name="MySqlUserTextBox" Margin="8,0,0,6" Text="root"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Password:" VerticalAlignment="Center"/>
                    <PasswordBox Grid.Row="2" Grid.Column="1" x:Name="MySqlPasswordBox" Margin="8,0,0,6"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Database:" VerticalAlignment="Center"/>
                    <TextBox   Grid.Row="3" Grid.Column="1" x:Name="MySqlDatabaseTextBox" Margin="8,0,0,6"/>

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Query:" VerticalAlignment="Top" Margin="0,4,0,0"/>
                    <TextBox   Grid.Row="4" Grid.Column="1" x:Name="MySqlQueryTextBox" Margin="8,0,0,0" AcceptsReturn="True" Height="80" Text="SELECT * FROM your_table LIMIT 100"/>
                </Grid>

                <TextBlock Foreground="#7F8C8D" FontStyle="Italic" Margin="0,6,0,0"
                           Text="When 'MySQL' is selected as Source, the run will POST JSON to /run-pipeline-json (no file upload)."/>
            </StackPanel>
        </Border>

        <!-- אזור עמודות ופעולות -->
        <Border Grid.Row="3" Style="{StaticResource ColumnPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="⚙️ הגדר פעולות עיבוד" Style="{StaticResource SectionHeader}"/>
                    
                    <!-- הודעה כאשר אין קובץ -->
                    <TextBlock x:Name="NoFileMessageTextBlock" 
                               Text="בחר קובץ כדי להציג את העמודות הזמינות" 
                               FontSize="14" 
                               Foreground="#95A5A6" 
                               HorizontalAlignment="Center" 
                               Margin="0,50,0,0"
                               Visibility="Visible"/>
                    
                    <!-- פאנל פעולות גלובליות -->
                    <Border x:Name="GlobalOperationsPanel" 
                            Style="{StaticResource ColumnPanel}"
                            Background="#F0F8FF"
                            Visibility="Collapsed"
                            Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="🌍 פעולות על כל הדאטה" 
                                       FontWeight="Bold" 
                                       FontSize="14" 
                                       Foreground="#1E3A8A"
                                       Margin="0,0,0,10"/>
                            
                            <WrapPanel Orientation="Horizontal">
                                <CheckBox Content="הסר שורות ריקות לחלוטין" 
                                          Margin="0,2,15,2"
                                          Tag="global|cleaning|remove_empty_rows"
                                          Checked="GlobalOperationCheckBox_Changed"
                                          Unchecked="GlobalOperationCheckBox_Changed"/>
                                <CheckBox Content="הסר שורות זהות" 
                                          Margin="0,2,15,2"
                                          Tag="global|cleaning|remove_duplicates"
                                          Checked="GlobalOperationCheckBox_Changed"
                                          Unchecked="GlobalOperationCheckBox_Changed"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- פאנל עמודות -->
                    <StackPanel x:Name="ColumnsPanel" Visibility="Collapsed">
                        <TextBlock Text="📊 פעולות לפי עמודה" 
                                   FontWeight="Bold" 
                                   FontSize="14" 
                                   Foreground="#1E3A8A"
                                   Margin="0,0,0,10"/>
                        <!-- עמודות יתווספו כאן דינמית -->
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- כפתורי פעולה -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,15,0,0">
            <Button Content="🔄 אפס הגדרות" 
                    Click="ResetSettings_Click" 
                    Style="{StaticResource ModernButton}" 
                    Background="#E74C3C" 
                    Margin="0,0,10,0"/>
            <Button Content="💾 שמור קונפיגורציה" 
                    Click="SaveConfig_Click" 
                    Style="{StaticResource ModernButton}" 
                    Background="#27AE60" 
                    Margin="0,0,10,0"/>
            <Button Content="▶️ הרץ Pipeline" 
                    Click="RunPipeline_Click" 
                    Style="{StaticResource ModernButton}" 
                    Background="#8E44AD" 
                    FontSize="14"/>
        </StackPanel>

        <!-- אזור תוצאות -->
        <Border Grid.Row="5" Style="{StaticResource ColumnPanel}" Margin="0,15,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ResultTextBlock" 
                           Text="התוצאות יוצגו כאן לאחר הרצת Pipeline" 
                           FontSize="12" 
                           Foreground="#2C3E50" 
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
