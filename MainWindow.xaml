<Window x:Class="PipeWiseClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PipeWise - ETL Client" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- סגנונות מותאמים אישית -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357ABD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ColumnPanel" TargetType="Border">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderBrush" Value="#E1E5E9"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <!-- כותרת ראשית -->
        <TextBlock Grid.Row="0" Text="PipeWise - מערכת עיבוד נתונים חכמה" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="#2C3E50" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- בחירת קובץ מקור -->
        <Border Grid.Row="1" Style="{StaticResource ColumnPanel}" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="📁 בחר קובץ מקור" Style="{StaticResource SectionHeader}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="FilePathTextBox" Width="600" Height="35" 
                             IsReadOnly="True" FontSize="12" 
                             VerticalContentAlignment="Center" 
                             Background="White" BorderBrush="#BDC3C7" BorderThickness="1"/>
                    <Button Content="Browse" Click="BrowseFile_Click" 
                            Style="{StaticResource ModernButton}" 
                            Margin="10,0,0,0" Height="35"/>
                </StackPanel>
                <TextBlock x:Name="FileInfoTextBlock" 
                           Text="לא נבחר קובץ" 
                           Foreground="#7F8C8D" 
                           FontStyle="Italic" 
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- אזור עמודות ופעולות -->
        <Border Grid.Row="2" Style="{StaticResource ColumnPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="⚙️ הגדר פעולות עיבוד" Style="{StaticResource SectionHeader}"/>
                    
                    <!-- הודעה כאשר אין קובץ -->
                    <TextBlock x:Name="NoFileMessageTextBlock" 
                               Text="בחר קובץ כדי להציג את העמודות הזמינות" 
                               FontSize="14" 
                               Foreground="#95A5A6" 
                               HorizontalAlignment="Center" 
                               Margin="0,50,0,0"
                               Visibility="Visible"/>
                    
                    <!-- פאנל עמודות (מוסתר בהתחלה) -->
                    <StackPanel x:Name="ColumnsPanel" Visibility="Collapsed">
                        <!-- עמודות יתווספו כאן דינמית -->
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- כפתורי פעולה -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,15,0,0">
            <Button Content="🔄 אפס הגדרות" 
                    Click="ResetSettings_Click" 
                    Style="{StaticResource ModernButton}" 
                    Background="#E74C3C" 
                    Margin="0,0,10,0"/>
            <Button Content="💾 שמור קונפיגורציה" 
                    Click="SaveConfig_Click" 
                    Style="{StaticResource ModernButton}" 
                    Background="#27AE60" 
                    Margin="0,0,10,0"/>
            <Button Content="▶️ הרץ Pipeline" 
                    Click="RunPipeline_Click" 
                    Style="{StaticResource ModernButton}" 
                    Background="#8E44AD" 
                    FontSize="14"/>
        </StackPanel>

        <!-- אזור תוצאות -->
        <Border Grid.Row="4" Style="{StaticResource ColumnPanel}" Margin="0,15,0,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ResultTextBlock" 
                           Text="התוצאות יוצגו כאן לאחר הרצת Pipeline" 
                           FontSize="12" 
                           Foreground="#2C3E50" 
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>